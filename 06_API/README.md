# 06_API

API documentation (Phase 2).

- REST: JSON, snake_case, ISO-8601 dates
- Auth: JWT + Supabase refresh tokens
- Versioning: `/v1/`, `/v2/` URL prefix
- Rate limiting: 100 req/min per household
