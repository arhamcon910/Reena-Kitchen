# 11_Infrastructure

Cloud infrastructure (Phase 2).

## Target Stack

```
Cloudflare → Kong API Gateway → Kubernetes
                                  └── NestJS microservices × 11
Supabase (PostgreSQL + Auth + Storage)
Redis (BullMQ + Cache)
```
